<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="ReceptionBot.Views.JournalView">
    <ScrollViewer>        
        <StackPanel Spacing="5">
            <TextBlock HorizontalAlignment="Center" FontSize="20" FontStyle="Oblique">Журнал</TextBlock>
            
            <Button HorizontalAlignment="Right" Command="{Binding ClearJournal}">Очистить</Button>
            
            <!--
            <DataGrid Items="{Binding JournalRecords}">                
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Дата и время" 
                                Binding="{Binding FormatedDateTime}" 
                                 />
                    <DataGridTextColumn Header="Событие" 
                                Binding="{Binding Text}" 
                                Width="*" />
                </DataGrid.Columns>
            </DataGrid>
            -->
        </StackPanel>
    </ScrollViewer>
</UserControl>